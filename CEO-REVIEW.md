# CEO Plan: cineseek

Generated by /plan-ceo-review — 2026-07-04
Mode: SELECTIVE EXPANSION
Repo: (greenfield, non-git) whsanha55 style

## 목적

벡터DB 학습용 토이 프로젝트. 첫 벡터DB.
핵심 학습 루프: **임베딩 → 색인 → 유사도검색**.

## 전제 도전 (0A)

- 맞는 문제인가: "의미 기반 영화 검색" 실수요 있음. 줄거리 임베딩 단일벡터 = 최소 신호로 최대 학습 → 옳은 시작점.
- 목적 명확화: 서비스 출시가 아니라 **벡터DB 학습**. 따라서 스코프는 학습초점 유지 우선.
- 아무것도 안 하면: 개인 프로젝트라 손해 0 → 스코프 자유도 큼.

## 구현 접근법 (0C-bis)

| 후보 | 요약 | 결정 |
|---|---|---|
| A: Python + API 임베딩 | FastAPI + OpenAI 임베딩 + Qdrant + PG | |
| **B: Python + 로컬 임베딩** | FastAPI + bge-m3/e5 + Qdrant + PG | ✅ 채택 |
| C: Java/Spring + API 임베딩 | 기존 oneMovieWepApp 재사용 | |

**채택: B.** 근거 = API 비용/의존 회피 + 인프라 소유 + con-jjong(OCI ARM CPU) 호스팅.
주의 = CPU 임베딩이라 배치 처리량 제한. v1 규모 1~3만건 권장.

## 스코프 결정 (SELECTIVE EXPANSION 체리픽)

| # | 제안 | 규모 | 결정 | 근거 |
|---|---|---|---|---|
| 1 | 개인화 추천 (본거/안본거) | L | **DEFER → 로드맵 R1** | auth·유저시스템 딸려와 학습초점 흐림. v1은 자리만 확보 |
| 2 | 장르 하이브리드 필터 (+감독/출연진) | S | **ACCEPT → v1** | 작고, payload 필터 = 벡터DB 핵심 학습 |
| 3 | 평가 하니스 (쿼리 10종 + top-5) | S | **ACCEPT → v1** | 벡터검색 품질은 눈에 안 보임, 회귀감지 필수 |

## Accepted Scope (v1에 추가됨)

- 하이브리드 필터: 시맨틱 + genre/year/rating **+ director/cast** (Qdrant payload)
- 평가 하니스: 고정 쿼리 10종, 실행 시 top-5 출력

## Deferred to 로드맵

- R1 개인화 추천 — 취향벡터 = 본 영화 임베딩 평균 → 안 본 것 유사검색. v1은 PG `user`/`watch_history` 스키마 자리만 확보, auth는 v2.
- R2 리뷰 임베딩 — 줄거리+리뷰 결합 신호. Qdrant 재색인 idempotent 유지.
- R3 포스터 이미지 검색 (CLIP 멀티모달) — 별도 컬렉션 분리 가능 구조.

## 핵심 불변식

- **PG = 진실의 원천(SoT).** Qdrant는 파생 인덱스 → 언제든 PG에서 재생성 가능(파이프라인 idempotent).
- payload에 필터 대상(genre/year/rating/director/cast) 사본 → Qdrant 단독 필터.
- movie_id 안정 PK 유지 → 개인화(R1) 자연 확장.

## 남은 열린 결정

1. bge-m3 vs multilingual-e5 (한국어 줄거리 비중 → bge-m3 다국어 강함). 임베딩 차원 확정 → Qdrant 컬렉션 설정에 필요.
2. 하이브리드: PG BM25 결합 vs Qdrant sparse vector.

## 리뷰 범위 주석

- Section 1(아키): v1 스케치 작성 (CONCEPT.md §8-ter).
- Section 2~10(에러맵·보안·성능·배포): 코드/스키마 확정 후 의미 있음 → DEFER. `/plan-eng-review`로.
- Section 11(디자인): SKIP (UI 없음, cineseek-ui 별도).

## 다음 스텝

1. 임베딩 모델 1개 픽 → 차원 확정.
2. PG 스키마 + Qdrant 컬렉션 설계 → docker-compose 스캐폴딩.
3. `/plan-eng-review`로 아키텍처/스키마 리뷰.
